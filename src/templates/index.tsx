import { MainVisual } from "./MainVisual";
import { Profile } from "./Profile";

export const Template: React.FC = () => {
  return (
    <main>
      <MainVisual />
      <Profile />
    </main>
  );
};
